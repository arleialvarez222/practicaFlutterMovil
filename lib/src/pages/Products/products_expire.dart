import 'package:flutter/material.dart';

class ProductsExpire extends StatelessWidget {
  const ProductsExpire({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("products expire"),
    );
  }
}